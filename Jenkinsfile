pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello Jldroid SRE"'
                sh '''
                    echo "Jenkins More Ireland Guiness !"
                    ls -lah
                '''
            }
        }
    }
}
